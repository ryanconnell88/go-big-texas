#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webpack-watch-server/bin/webpack-watch-server.js" "$@"
  ret=$?
else 
  node  "$basedir/../webpack-watch-server/bin/webpack-watch-server.js" "$@"
  ret=$?
fi
exit $ret
